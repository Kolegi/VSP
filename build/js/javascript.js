$(document).ready(function () {
  $(".slider").slick({
    autoplay: true,
    autoplaySpeed: 4000,
    infinity: true,
    speed: 1000,
    // adaptiveHeight: true,
    easing: "ease",
    dots: true,
  });
});
